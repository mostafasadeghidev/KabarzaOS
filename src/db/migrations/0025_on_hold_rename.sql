-- «در انتظار» → «متوقف»
--
-- ⚠️ چرا: نامِ فارسیِ این وضعیت (On Hold) درست کنارِ «آماده برای بررسی»
-- می‌نشیند و هر دو «منتظر» خوانده می‌شوند؛ کاربر نمی‌فهمد کدام یعنی «منتظرِ
-- تأییدِ کارفرما» و کدام یعنی «کار خوابیده». معنیِ واقعی «متوقف/بلاک‌شده»
-- است و همین نام هم گذاشته می‌شود.
--
-- ⚠️ فقط وقتی نام هنوز همان پیش‌فرض است: اگر تیم خودش اسمش را عوض کرده،
-- انتخابِ او دست نمی‌خورد. تسک‌های موجود هم دست‌نخورده می‌مانند — همان تگ
-- است، فقط نامش عوض می‌شود.
UPDATE tags
SET name = 'متوقف',
    name_i18n = COALESCE(name_i18n, '{}'::jsonb) || '{"fa": "متوقف"}'::jsonb
WHERE slug = 'on-hold'
  AND type = 'task_status'
  AND name = 'در انتظار';
