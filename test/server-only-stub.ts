/**
 * جایگزینِ بسته‌ٔ `server-only` در تست.
 *
 * ⚠️ آن بسته عمداً در محیطِ Node ِ خالی resolve نمی‌شود؛ کارش فقط این است که
 * وارد‌کردنِ فایل در کلاینت را در زمانِ ساخت بشکند. در تست نه کلاینتی هست و
 * نه باندلری، پس یک ماژولِ خالی همان معنا را دارد و رفتارِ محصول را عوض
 * نمی‌کند.
 */
export {};
